# Global settings
global:
  clusterName: "duploinfra-{{infraName}}"
  duploAuthUrl: ""
  awsRegion: ""
  customerName: ""
  environment: ""
  namespaceFilter: ".+otel.+"
  release: "2.0.7"
  grafanaProxyUrl: ""
  slackWebhookUrl: ""
  gpgPassphrase: ""
  grafanaPassword: ""
  
# Grafana UI settings
grafanaUI:
  enabled: true
  replicas: 1
  image: "duplocloud/otel-dashboards"
  imageTag: "{{ .Values.global.release }}"
  resources:
    limits:
      cpu: "100m"
      memory: "512Mi"
    requests:
      cpu: "100m"
      memory: "512Mi"
  volume:
    size: "10Gi"
  plugins: "grafana-exploretraces-app,yesoreyeram-infinity-datasource,volkovlabs-form-panel"

# Duplo Automation settings
duploAutomation:
  enabled: true
  replicas: 1
  image: "duplocloud/duplo-automation"
  imageTag: "{{ .Values.global.release }}"
  volume:
    size: "10Gi"

# Grafana Proxy settings
grafanaProxy:
  enabled: true
  replicas: 1
  image: "duplocloud/sso-proxy"
  imageTag: "v2.0.3-otel"
  resources:
    limits:
      cpu: "50m"
      memory: "128Mi"
    requests:
      cpu: "50m"
      memory: "128Mi"

# Secrets
secrets:
  grafanaUI:
    username: "duplocloud"
    password: "{{ .Values.global.grafanaPassword }}"
  gpgPassphrase: "{{ .Values.global.gpgPassphrase }}"

# Integration Job settings
integrationJob:
  enabled: true
  image: "alpine/k8s"
  imageTag: "1.32.1"

# Observability Collector CronJob settings
observabilityCollector:
  enabled: true
  schedule: "0 0 * * *"
  image: "duplocloud/duplo-observability"
  imageTag: "stable"
  jobVersion: "2.1.0" 